/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.bing.com','lh3.googleusercontent.com'],
  },
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = nextConfig
